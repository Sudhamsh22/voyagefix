rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: This is for demonstration purposes and is not secure.
    // In a production app, you would use Firebase Authentication and write
    // security rules to ensure users can only access their own data.
    match /trips/{tripId} {
      allow read, write: if true;
    }
  }
}
